@import "shared"

// Change the box model for Mozilla, Webkit, IE8 and the future
//
// @param $bs
//   [ content-box | border-box ]
box-sizing($bs)
  experimental(box-sizing, unquote($bs), 'moz', 'webkit', not 'o', not 'ms', not 'khtml', 'official')
